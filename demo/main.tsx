import React from "react";
import ReactDOM from "react-dom/client";
import { RemoveBgTool } from "../src";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <RemoveBgTool accent="#3d5afe" appName="누끼컷" model="ormbg" />
  </React.StrictMode>,
);
